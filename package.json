{
  "name": "neon-runner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "NEON_RUNNER — Cyberpunk Infinite Runner",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist --dest r170 --add"
  },
  "dependencies": {
    "meshoptimizer": "^0.21.0",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^6.0.0"
  }
}
